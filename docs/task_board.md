# 任务协作板（三任务共用共读日志）

> 规则（详见 docs/three_tasks_plan.md §4）：
> 1. 每次会话开始**必读本文件末尾 30 行**；结束**必追加**一条本任务条目；
> 2. 条目格式：`[任务名] 日期 | 状态 | 本会话产出 | 需要别人做的事`；
> 3. 跨任务请求：`REQ[发起方→接收方] 内容/原因/期望`；完成方回 `DONE[任务名]`；
> 4. 接口契约变更先发 `BREAK[任务名]` 说明，不得先斩后奏；
> 5. 共用文件（assets/i18n/*.json、event_bus_types.h）只追加，禁改删他人键/定义；
> 6. 禁止删除或改写他人条目；
> 7. 无板开发：真机测试指导统一写 docs/hardware_test_guide.md，
>    每任务只更新自己章节（[HUB]=§1/§2 [GUI]=§3 [WEB]=§4，§5 append-only）。
>
> 任务名与范围：
> [HUB] 数据中枢 = 统一数据接口(i18n/sensor_cache/time_svc/sysinfo)
>                  + T1 ath30 收口 + T4 lora_tp（子批次①②③）
> [GUI] 板端体验 = T2 音频升级(audio_module/audio_player)
>                  + LVGL 蓝白主题界面（子批次①②）
> [WEB] Web 界面 = Vue 重构 + 只读 API 路由 + OTA 上传界面

---

[HUB] 2026-09-09 | 计划中 | 尚未开始；建议先交批次①四组件骨架（哪怕桩） | 无
[GUI] 2026-09-09 | 计划中 | 尚未开始；建议先做子批次①音频（不依赖 HUB） | 等 [HUB] 批次①（未交付前 bridge/路由用 mock）
[WEB] 2026-09-09 | 计划中 | 尚未开始 | 等 [HUB] 批次①（未交付前路由回 mock JSON）
[GUI] 2026-09-10 | 批次①进行中 | 声明+产出预告：①event_bus_types.h 已按 I8 追加播放服务事件（追加声明见此）：EVENT_AUDIO_PLAY_STARTED=0x0007 / EVENT_AUDIO_PLAY_PROGRESS=0x0008 / EVENT_AUDIO_PLAY_FAILED=0x0009（新增），EVENT_AUDIO_PLAY_DONE 复用既有 0x0002（原无载荷无使用者，grep 已核实），统一载荷 event_audio_play_t{token,err,duration_ms,position_ms}，进度事件节流 ≥10%（每跨 10% 台阶最多一条）；②ovs.dtb.json 已追加顶层 audio 策略节点（compatible=ovs-audio-policy：sd_pin/volume_default/buf_frames/queue_depth，DTREE 读取+缺省兜底），amplifier 既有 sd_pin 键不动；③audio_module 异步化+audio_player 新组件已实现，ovs_tests 音频门禁全绿（185/185，mock i2s 覆盖 §2.4 全场景）；idf.py build 验证进行中 | 无（全部在 GUI 域内，未动他人文件；audio_player NVS 用命名空间 "audio_player"）
[WEB] 2026-09-10 | 已交付（待固件侧编译确认） | ①Vue 前端全量重构：src/{api,components,composables,i18n,router,stores,views} 模块化 + 自研 hash 路由与轻量 store（未引新依赖）+ 蓝白主题（主蓝 #1E6FFF/白卡片/#F5F7FA 底）；②i18n 双语 zh-CN/en-US，语言包 fetch('/i18n/<lang>.json') 与 LVGL 同源、缺键回退英文再回退键名；键名按契约 I1 扁平大写下划线，附录 A 全量种子键+WEB 扩展键已内置 vue-ui/src/i18n/locales/*.json（schema {lang,ver,strings}）；③四页面：Dashboard（/api/sensor 30s 轮询+手动刷新、/api/net/info、/api/time）、Network（wifi/scan|connect|status + net/mode 界面化）、Firmware（前端三重校验 .bin/≤分区/魔数 0xE9|OVSO → XHR 上传 /api/ota/firmware → 轮询 /api/ota/status → 重启倒计时→探测上线→新版本展示）、Settings（语言切换+占位）；④后端 additive：modules/web/web_api_sysinfo.c 按契约 I5 实现 GET /api/sensor、/api/net/info、/api/time、/i18n/<lang>.json（SPIFFS 白名单读取防穿越），HUB 未交付前路由内 mock JSON（WEB_API_USE_HUB=0，CMake 预留开关+REQUIRES 注释，交付后一键切真）；仅 web.c 追加 2 行 init 调用与 CMakeLists 1 行 SRCS（仿 web_ota 模式）；⑤web_data 已用既有 fs_to_c.py 链路重新生成（125KB<2MB SPIFFS 限额）；⑥vite dev(mock 全页面联调)+vite build 本机通过；本机无 ESP-IDF，idf.py build 未跑——C 侧已做 gcc -fsyntax-only 门禁且为纯 additive | ①[HUB]：批次①交付后翻转 WEB_API_USE_HUB=1 并补 REQUIRES sensor_cache sysinfo time_svc（web_api_sysinfo.c 注释已写明；错误枚举名若非 SENSOR_OK/SYSINFO_OK/TIME_SVC_OK 只需微调对应分支）；②[HUB]：assets/i18n 请以 vue-ui/src/i18n/locales/*.json 为种子合并（含 WEB 域键中英译文）；③项目所有者：构建主机跑一次 mybuild.sh --no-flash 验证固件编译，上板测试按 hardware_test_guide §4
[GUI] 2026-09-10 | 批次①②完成 | ①音频：audio_module 异步化（write_async 环形缓冲+tasker 喂数/set_volume/mute(SD 脚)/drain/abort/完成回调，录音路径未动）+ 新增 audio_player（队列≥3/OPCM 文件播放/audio_decoder_t 挂点 v1 passthrough/音量 NVS "audio_player" 命名空间/token 查询）；事件追加已按前条声明落地；②UI：六层全实装（蓝白主题 #1E6FFF/#F5F7FA/白卡片、ui 控件 5 种、navigator 注册表+栈+I6 navigator_set_standby、home/settings/standby 三页、bridge I1~I4 函数表 mock/真身双开关、presenters 事件驱动刷新）；旧 nav.c/page_home 骨架被取代（nav.h/bridge_time 已删）。验证：ovs_tests 234/234 绿（mock i2s 音频 96 项 + nav_stack 49 项）+ idf.py build 绿（容器 esp-idf v6.0.1 实跑，ovs.bin 余 39%）+ PC 模拟 build 绿且 Xvfb 实跑 8s 无错 + ovs_ui_smoke 17/17（导航/语言热切换/mock 数据）；三层零业务头 grep 自检通过；development_log 顶部与 hardware_test_guide §3 已更新 | 需要 [HUB]：交付 I1~I4 后我在 src/lvgl/CMakeLists.txt 置 OVS_BRIDGE_HUB_REAL=1 接真（现=0 全 mock）；REQ[GUI→HUB] 无新增接口需求，音频事件/audio 节点均在 GUI 域内自闭环
[HUB] 2026-09-10 | 批次①完成+集成收口（三方接真，全绿） | ①契约 I1~I4 四组件交付：components/core/i18n（cJSON 解析 /i18n/<lang>.json、热切换、未命中回退原文+节流 LOGW、附加 i18n_set_base_path/deinit）；components/core/sensor_cache（订阅 EVENT_SENSOR_TEMP_HUMIDITY/ERROR，float→milli 缓存，锁+时基经 sensor_cache_port 移植层）；components/core/time_svc（uptime 时钟 synced 恒 false，NTP 桩 time_svc_ntp_enable/set 返回 FAIL）；components/modules/sysinfo（只读封装 net_mgr_get_status + esp_netif 查 ip/netmask/gw/mac，错误枚举即 SENSOR_OK/SYSINFO_OK/TIME_SVC_OK 体系，未改 net_mgr/wifi 任何文件）；②assets/i18n/{zh-CN,en-US}.json（92 键=WEB 种子 90+GUI 域 HOME_NET/STANDBY_HINT，ver=2，双语键集一致）；SPIFFS 打包改为 build 暂存目录（ovs.dtb.json + i18n/ 同镜，configure 期 file(COPY)，见根 CMakeLists.txt）；③集成收口（GUI/WEB 授权的两处翻转已完成）：src/lvgl OVS_BRIDGE_HUB_REAL=1+REQUIRES 补 i18n/sensor_cache/sysinfo/time_svc；modules/web WEB_API_USE_HUB=1+REQUIRES 同步补；app_init [批4.5] 注册 i18n(依赖 net_stack 内 spiffs 挂载序)/time_svc/sensor_cache/sysinfo（均 optional 缺席降级）；④他域最小修复（逐条报备）：src/lvgl/bridge/bridge.c 接真后 mock 存储变量未用触发 -Werror，将 s_mock_volume/s_mock_lang/词表分别用 !OVS_BRIDGE_AUDIO、!OVS_BRIDGE_HUB_REAL 宏包裹，零逻辑改动；⑤验证（本机实跑）：ovs_tests 308/308 绿（新增 test_hub.c I1~I4 共 74 项+net_mgr_mock）、idf.py build 绿（ovs.bin+spiffs.bin，暂存目录确认含 i18n/）、PC 模拟 build 绿、ovs_ui_smoke 17/17 | DONE[HUB→GUI]：I1~I4 已交付且 OVS_BRIDGE_HUB_REAL=1 已由本次代翻（GUI 无需再动，下次烧录即接真；枚举与 mock 分支签名逐一对齐）；DONE[HUB→WEB]：WEB_API_USE_HUB=1 已翻+REQUIRES 已补，错误枚举恰为 SENSOR_OK/SYSINFO_OK/TIME_SVC_OK 预写名，web_api_sysinfo.c 零改动；i18n 语言包已按 locales/*.json 合并（追加 HOME_NET/STANDBY_HINT 两键，ver=2，发布路径 /i18n/<lang>.json）| 需要：项目所有者上板回归（hardware_test_guide §1/§2.1 已写）；[GUI]/[WEB] 下次会话确认接真后表现
[HUB] 2026-09-10 | 批次②完成（ath30 收口），批次③待作 | ①init 自动启动周期采集（tasker Middle）：间隔=设备树 sample_interval_ms→兼容 measure_interval_ms→缺省 30000+LOGW（dtb 已追加 sample_interval_ms:30000）；②失败降级：连续 3 次失败→发一次 EVENT_SENSOR_ERROR+停采，之后每 5 周期重试一次成功即恢复（原实现每次失败发 ERROR 且永不恢复）；③mock i2c 三场景单测（正常/无应答/CRC 错+停采窗口/恢复）入 ovs_tests [HUB-T1] 12 项，另修复 tests/dtree_mock.c set_int 节点指针不匹配（测试基建）；④事件载荷：既有 float 载荷维持不变（只追加原则），milli 由 sensor_cache 消费层换算——对 §3"milli 载荷"条款的偏差说明：换算点下移至 sensor_cache，事件协议未变，消费者（GUI bridge/WEB 路由）零感知 | 验证：ovs_tests 325/325 绿 + idf.py build 绿（app 余 22%）+ PC 模拟绿 | 下一步：批次③ lora_tp（lora_transport_design §9），真机项已写入 hardware_test_guide §2.2
[HUB] 2026-09-10 | 批次③完成（lora_tp）——[HUB] 三批次全部交付 | ①components/modules/lora_tp 新组件：§2 公共 API 冻结集+依赖注入（lora_drv_api_t/tick/mem）；帧=协议 §2.1 + 消息子头扩展 12B（+CRC32）；RELIABLE 滑窗 ARQ（收端只按序落盘→CRC32 可链式增量，RAM/sink 路径统一；整窗重试耗尽 FAILED(TIMEOUT)；对端丢状态回退续传+交付幂等记忆防重复）；UNRELIABLE 单帧直通（广播仅限此 QoS，可靠广播 ERR_PARAM）；②event_bus_types.h 追加 EVENT_LORA_TP_TX_DONE/TX_FAILED/RX_COMPLETE/PROGRESS=0x0005~0x0008（载荷 event_lora_tp_t，PROGRESS 5% 节流；0x0004 被 EVENT_LORA_READY 占用故顺延）——[GUI] 未来聊天页订阅这四个事件即可，无需 include lora_tp.h（I7 语义）；③设备树 lora.lora_tp 节点已落（compatible=lora-tp，档位数值为手册核实前占位，见 §2.3.4 清单）；④app_init 注册 lora_tp（optional，lora 驱动一并拉起缺席降级，10ms tasker Little tick）；⑤单测：loop mock 双实例回环覆盖 §7 六场景（8KB 逐字节/30% 丢帧 ARQ/断链续传/CRC 拒收 PEER/广播规则/sink 路径）63 项 | 验证：ovs_tests 388/388 绿 + idf.py build 绿（app 余 21%）+ PC 模拟绿 | 需要别人做的事：[GUI] 聊天页/未读角标接入时订阅 EVENT_LORA_TP_*（现成的四事件）；项目所有者上板联调需两台设备且其中一台 local_address 手改为 2（hardware_test_guide §2.3 已写）；[HUB] 任务关闭，后续经 board REQ
[HUB] 2026-09-10 | 结构调整 | ①应用户要求 lora_tp 三文件迁入 components/modules/lora（组件级合并：lora.c=链路驱动 / lora_tp.c=传输服务，对外仍只暴露 lora_tp.h，I7 契约不变，GUI 订阅 EVENT_LORA_TP_* 用法不变）；lora 组件 CMake 合并 SRCS/REQUIRES，根注册与 main REQUIRES 同步清理；②仓库全局 ath30 命名（模块实际名）已对齐测试侧：test_ath30.c/dtree_mock compatible/日志关键字 [ath30] | 验证：ovs_tests 388/388 + idf.py build + PC 模拟全绿 | 无
[HUB] 2026-09-10 | 真机 bring-up | ①真机烧录+OTA 双轮验证：14/16 模块 READY（UI/Web 真数据/i18n/lora_tp/时间/传感器缓存/显示背光），i18n 被 web 部署抹掉的问题已修（语言包并入 web_data，mybuild.sh 自动拷贝）；②st7789 已迁移 Demo 的 ST7789V 出厂序列；③新组件 gt967（GT9xx 系触控，16 位寄存器/复位选址/事件发布同 cst816s），设备树 touchscreen→gt967-touch；④阻塞：I2C 总线全地址扫描无 ACK（ath30+GT967 同时 NACK+超时），待所有者检查 SDA=16/SCL=17 焊接、上拉、供电；W25Q128 新片需 OVS_MEDIA_FORMAT_ON_FIRST_BOOT=1 格式化一次 | 需要所有者：I2C 硬件排查后告知，我这边驱动/诊断已就绪（扫描代码保留在 gt967 探测失败路径）
